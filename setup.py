from distutils.core import setup

setup(name='map-annotation',
      version='1.0',
      packages=['map_annotation'],

      # metadata for upload to PyPI
      author="Lucas Chiesa",
      author_email="lucas@creativa77.com.ar",
      description="Experiments on crowd sourcing map tagging",
      license="BSD",
      keywords="mapping robotics crowd-source",
      )
